services:
  paic:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
    image: ghcr.io/mareox/pan-paic:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./profiles:/app/profiles
